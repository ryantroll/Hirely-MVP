 // Define helper functions to handle Mongoose functions

 function ObjectId(str) {
    return str;
 }

 function ISODate(str) {
    return new Date(str);
 }

 var usersBak = [
        /* 1 */
        { // paste your user backup here
        }
]

module.exports = usersBak;